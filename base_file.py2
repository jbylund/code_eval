#!/usr/bin/python
from sys import argv

def main():
  with open(argv[1], 'r') as infile:
    for test in infile:
      test = test.strip()
      if(0 == len(test)):
        continue
      print test


if "__main__" == __name__:
  main()

